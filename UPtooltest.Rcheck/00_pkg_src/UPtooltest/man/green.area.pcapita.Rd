% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/green.area.pcapita.R
\name{green.area.pcapita}
\alias{green.area.pcapita}
\title{This function calculates the total green area within the urban area
(forests, parks, gardens, etc.) per inhabitant.}
\usage{
green.area.pcapita(ga, pop)
}
\arguments{
\item{ga}{raster layer or shapefile that contains the polygons for green areas}

\item{pop}{raster of population distribution or numeric value with information of total population}
}
\value{
a data frame with information about the infill areas in square kilometers
}
\description{
METHOD:
The park and public space area available per person (green.area.p) is estimated
by adding up the area with vegetation (green.area), in square meters, in the parks
and public open spaces of the urban area, and dividing this result (green.area.t) by the total population (t.pop).
}
\examples{
library(raster)

land.cover2025 <- system.file("extdata", "Land_cover.tif", package = "UPtooltest")
land.cover2025 <- raster::raster(land.cover2025)
pop.base <- system.file("extdata", "POP_2025.tif", package = "UPtooltest")
pop.base <- raster::raster(pop.base)

green.land <- land.cover2025
green.land[green.land !=  8]<- 0
green.a.capita <- green.area.pcapita(green.land, pop.base)
}
