% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobs.proximity.R
\name{jobs.proximity}
\alias{jobs.proximity}
\title{This function calculates the percentage of the population that lives within
1,000 m from the areas with high job density in the urban area.}
\usage{
jobs.proximity(jobs, pop)
}
\arguments{
\item{jobs}{a raster that contains information about jobs distribution}

\item{pop}{a raster that contains information about population distribution}
}
\value{
a data frame with information about the number and percentage of population with proximity to job hubs
}
\description{
METHOD:
This indicator identifies the areas of the urban area that concentrate employment and
then quantifies the population that lives close to these areas as a percentage of the total population.
}
\examples{
library(raster)

pop.b <- system.file("extdata", "POP_2025.tif", package = "UPtooltest")
pop.b <- raster::raster(pop.b)

jobs <- system.file("extdata", "Jobs.tif", package = "UPtooltest")
jobs <- raster::raster(jobs)

jobs.prox <- jobs.proximity(jobs, pop = pop.b)
}
